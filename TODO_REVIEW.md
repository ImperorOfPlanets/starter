# TODO Review — Starter Project

## КРИТИЧНЫЕ (безопасность)

### ✅ МОГУ ИСПРАВИТЬ БЫСТРО:
1. **Нет авторизации на POST** — добавить проверку `session.get('logged_in')` в `routes.py` на все секции кроме `auth` и `language`
2. **tray.php → tray.py** — переименовать файл
3. **taskkill /F /IM pythonw.exe** — убивать только PID стартера, не все pythonw
4. **`占用此端口的`** — заменить на русский

### ⚠️ НУЖНА ПРОДУМАННАЯ РАБОТА:
5. **Произвольный import** — whitelist разрешённых секций
6. **SHA-256 без соли** — нужна миграция на bcrypt
7. **CSRF** — нужна библиотека или токены

## ВЫСОКИЕ

### ✅ МОГУ ИСПРАВИТЬ БЫСТРО:
8. **`SESSION_COOKIE_SECURE: False`** — ставить `True` если SSL активен
9. **Локкаут 5 сек → 60 сек** с прогрессивным бэкапом
10. **Firewall/settings без аутентификации** — блокировать в `handle_sections()`
11. **Бэкап пароля в консоль** — убрать вывод хэша
12. **DEBUG-принты** — убрать или в логгер

### ⚠️ НУЖНА ПРОДУМАННАЯ РАБОТА:
13. **`.env.example` с реальными OAuth-кредами** — заменить на placeholder
14. **MAC-адрес телеметрия** — добавить consent/ opt-out

## СРЕДНИЕ

### ✅ МОГУ ИСПРАВИТЬ БЫСТРО:
15. **`language.py` → несуществующий `create_env_with_defaults`** — исправить импорт
16. **Дубликаты ключей в `ru.py`** — удалить
17. **`process.py` bare except** — заменить на конкретные исключения
18. **Сессии 30 дней → 7 дней**
19. **Copyright 2025 → 2026**

### ⚠️ НУЖНА ПРОДУМАННАЯ РАБОТА:
20. **Планировщик — нет фонового потока** — добавить `threading.Timer` или `APScheduler`
21. **Неполные переводы en/cn** — добавить недостающие секции
22. **Нет Windows-оверрайдов** для firewall, ssl — нужна платформенная логика

## ДИЗАЙН / UX

### ✅ МОГУ ИСПРАВИТЬ БЫСТРО:
23. **scheduler секция невидна** — добавить `this_section_in_control_panel`
24. **Дублирование `t()`** — вынести в utility
25. **Debug print в starter.py** — убрать

### ⚠️ НУЖНА ПРОДУМАННАЯ РАБОТА:
26. **Watchdog в сервисном режиме** — добавить health check
27. **Нет тестов** — нужен pytest + fixtures

---

## ОЦЕНКА ВРЕМЕНИ

### БЫСТРОЕ (20-30 мин):
- Пункты 1, 2, 3, 4, 8, 9, 15, 16, 17, 18, 19, 23, 25

### СРЕДНЕЕ (30-60 мин):
- Пункты 5, 10, 11, 12, 13, 20, 24

### СЛОЖНОЕ (1-2+ часа):
- Пункты 6, 7, 14, 21, 22, 26, 27

### ИТОГО: За 2 часа реально покрыть ~70% (быстрое + среднее)
