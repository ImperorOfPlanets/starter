Здесь описываем ошибки инстуркции установки на сайте
Формат:
ОС ВЕРСИЯ ОПИСАНИЕ ОШИБКИ

Ubuntu 22.04

Перед созданием папки нужно посмотреть где находиться команда unix pwd
Должно быть app
Рассмотреть вариант что такой папки нет!  APP появляеться потому, что виртуальная машина
mkdir -p /app/starter
cd /app/starter

Исправить распаковку в нужную папку
# Распаковка архива и удаление zip-файла
unzip starter.zip -d /app/starter
rm starter.zip

Доработать стрикет запуска на проверку python или python3 и исходя из перменной запустить starter.py

PYTHON_CMD=$(command -v python3 || command -v python) && [ -z "$PYTHON_CMD" ] && echo "Ошибка: Python не найден!" && exit 1 || $PYTHON_CMD -c "import sys; exit(0) if sys.version_info[0] >= 3 else (print('Требуется Python 3!'), exit(1))" && exec $PYTHON_CMD starter.py